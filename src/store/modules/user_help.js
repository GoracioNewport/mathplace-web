export default class User {
  constructor (id, lastTheme, money, right, submit, like) {
    this.id = id
    this.lastTheme = lastTheme
    this.money = money
    this.right = right
    this.submit = submit
    this.like = like
  }
}
